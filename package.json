{
  "name": "nuxt-test",
  "version": "1.0.0",
  "description": "My supreme Nuxt.js project",
  "author": "got.wong",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "mock": "mocker ./api/mock",
    "build:gh-pages": "NODE_ENV=deploy nuxt build",
    "generate:gh-pages": "NODE_ENV=deploy nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.10.2",
    "@nuxtjs/dotenv": "^1.4.1",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^1.0.0",
    "cross-env": "^7.0.2",
    "push-dir": "^0.4.1",
    "mocker-api": "^2.9.5",
    "mockjs": "^1.1.0"
  }
}
